# refs/

Drop the Carbon/Rust/Unity reference DLLs here to enable the real compile
(`check.sh` tier 2). These ship with your server install and are **not**
redistributable, so `*.dll` here is git-ignored. See ../README.md.
